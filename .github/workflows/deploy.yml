name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:  # 수동 실행 허용

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # 빌드 및 검증 작업
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      # Skip Node.js setup and tests until implementation phase
      # Uncomment when package.json and tests are ready
      # - name: Setup Node.js
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: '18'
      #     cache: 'npm'
      
      # - name: Install dependencies
      #   run: npm ci
      
      # - name: Run tests
      #   run: npm test
      
      # - name: Run linter
      #   run: npm run lint

      # For now, just validate HTML exists
      - name: Validate HTML files
        run: |
          if [ ! -f "index.html" ]; then
            echo "Error: index.html not found"
            exit 1
          fi
          echo "✅ index.html found"
      
      # - name: Check JavaScript syntax
      #   run: |
      #     echo "Checking JavaScript files..."
      #     if [ -d "js" ]; then
      #       npx eslint js/**/*.js --no-eslintrc --parser-options=ecmaVersion:2020 || echo "ESLint check completed"
      #     fi
      
      - name: Setup GitHub Pages
        uses: actions/configure-pages@v4
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'
  
  # GitHub Pages 배포 작업
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
      
      - name: Output deployment URL
        run: |
          echo "🚀 Deployed to: ${{ steps.deployment.outputs.page_url }}"
